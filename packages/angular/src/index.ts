/**
 * ScrollInfinite Angular - Angular directives for infinite scroll
 * @packageDocumentation
 */

export { InfiniteScrollDirective } from './infinite-scroll.directive';

// Re-export core types for convenience
export type {
  ScrollInfiniteConfig,
  ScrollDirection,
  LoadingState,
} from 'scrollinfinite';
